# AIに手伝ってもらって導入する

ClaudeやChatGPTなどのAIを使っている方は、**このファイルの内容をまるごとコピーして
AIに貼り付ける**と、対話しながら導入できます。

Claude Code など、あなたのPCを操作できるAIなら、次のように頼むだけで進みます。

```
https://github.com/Doushimasho/petcam の AI_SETUP.md を読んで、
ペットカメラの導入を手伝ってください。
```

---

以下はAIへの指示です。ここから下をAIに渡してください。

---

## あなたの役割

利用者が「余っているスマートフォンをペットカメラ（見守りカメラ）にする」のを
最後まで手伝ってください。利用者はネットワークやコマンド操作に詳しいとは限りません。
専門用語は使うたびに一言で説明してください。

リポジトリ: https://github.com/Doushimasho/petcam

## 最初に確認すること

1. 利用者のOS（Windows / macOS / Linux）
2. Python 3.10以上が入っているか（`python --version`）
3. **カメラにする端末はAndroidか iPhoneか**
   - Androidを強く推奨。iPhoneは画面ロックやアプリ切替でカメラが止まる
   - iPhoneしかない場合は、その制約を先に伝えること
4. PCとカメラ端末が同じWi-Fiにいるか（ゲストネットワークは不可）

## 導入の手順

### 1. コードを置く

`git clone https://github.com/Doushimasho/petcam.git`
Gitが無ければ、GitHubの緑の Code ボタン → Download ZIP を案内する。

### 2. 起動する

- **Windows**: `start.bat` をダブルクリックするだけ。
  必要な部品は自動で入る。Pythonが無ければその場で案内が出る
- **macOS / Linux**: `pip install -r requirements.txt` のあと `python server.py`

起動すると、PINとアクセス用のURLがコンソールに表示される。
**このコンソールは閉じないよう伝えること**（閉じるとカメラも止まる）。

初回はOSがネットワークの許可を求める。
Windowsでは「プライベートネットワーク」を許可しないとスマホから繋がらない。

### 3. カメラにするスマホを設定する

1. PCと同じWi-Fiにつなぐ
2. Chromeで、表示されたURLの `/camera` を開く
3. 証明書の警告が出る → 詳細設定 → 安全でないページに移動
   （自己署名証明書のため。手順5で消える）
4. PINを入力
5. **CAMERA ON を押してカメラを許可**（許可を求められるのはこの1回だけ）

### 4. 見る

別の端末のブラウザで `/viewer` を開き、同じPINを入力する。

### 5. 外出先から見る（任意）

Tailscale（無料）を使う。**自宅をインターネットに公開しない方法**を選ぶこと。

1. PC・カメラ端末・視聴端末の3つにTailscaleを入れ、同じアカウントでログイン
2. 管理画面の DNS → HTTPS Certificates を有効にする
3. サーバを起動し直す

サーバが正式なHTTPS証明書を自動取得し、証明書の警告が出なくなる。
証明書は自動更新されるので、利用者が管理する必要はない。

## 先回りして伝えるべき、詰まりやすい点

**1. カメラ端末にTailscaleを入れ忘れる**
最も分かりにくい失敗。状態表示は `DEVICE ONLINE` `CAMERA ON` と正常なのに
映像だけ来ない。映像はサーバを経由せず端末同士が直接やりとりするため、
サーバに繋がるだけでは足りない。

**2. Androidのバッテリー最適化**
設定 → アプリ → Tailscale → バッテリー → 「制限なし」。
外さないとOSがVPNを切る。家では映るのに外出先で繋がらない形で出る。

**3. ゲストネットワーク**
端末同士の通信が遮断されているため繋がらない。

**4. Windows PowerShell では `&&` が使えない**
コマンドを渡すときは1行ずつに分けること。

**5. Windowsのコンソールをクリックすると出力が止まる**
対策済みだが、古い版では起きる。

## 動作確認

```
python selftest.py
```

26項目を確認する。映像そのものは実機でしか確かめられない。

## 絶対にやらないこと

- **`config.json` と `certs/` の中身を、外部に送ったり画面に貼ったりしない。**
  PINと秘密鍵と証明書が入っている
- **PINを公開の場所に書かない**
- **ルーターのポート開放でインターネットに直接公開しない。**
  外から見たい場合はTailscaleを使う
- 利用者に断りなく、ファイルをどこかへアップロードしない

## 仕組み（利用者に聞かれたら）

映像はサーバを通らず、カメラ端末と視聴端末が直接やりとりする（WebRTC）。
サーバの役割は、お互いを引き合わせることと状態を伝えることだけ。
そのため遅延が小さく、PCの負荷もほとんどない。
